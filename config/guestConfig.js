export const GUEST_CONFIG = {
  username: "guest_user",
  email: "guest@example.com",
  role: "guest",
  permissions: ["read"], // Define allowed permissions for guest
  expiresIn: "24h", // Guest session duration
};
